# Relayer wallet private key (hot wallet, keep secret, never commit)
RELAYER_PRIVATE_KEY=0x...

# Monad Testnet RPC
MONAD_RPC_URL=https://testnet-rpc.monad.xyz/

# The address that receives publisher revenue (set at deploy time or via registerSlot)
PUBLISHER_ADDRESS=0x...

# Filled in after deployment - hand to web/ team
AD_EXCHANGE_ADDRESS=0x...
